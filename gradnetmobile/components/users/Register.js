import { View } from "react-native";
import { Text } from "react-native-paper";

const Register = () => {
  return (
    <View>
      <Text>Đăng ký</Text>
    </View>
  );
};

export default Register;
